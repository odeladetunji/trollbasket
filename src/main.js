import 'bootstrap/dist/css/bootstrap.min.css';
import "@babel/polyfill";
import "core-js/modules/es6.promise";
import "core-js/modules/es7.array.includes";
import React, {Component} from 'react';
import ReactDOM from "react-dom";

class App extends Component {
     render(){
       return (
             <div>something is wrong
             <p>checking if hot reload is working</p>
             <p>Its really really working. This was really
             time taken and tasking. I have to go through the documentation thoughroughly It kind of so CYAOUCARwExnEixLdB6sDm2dIJ
             . To be sincere it was not funny at first</p>
             </div>
       );
     }
}

ReactDOM.render(<App />,
        document.getElementById('root'));
